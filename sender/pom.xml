<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>com.gfi.ozg.fitko</groupId>
    <artifactId>fitconnect-samples</artifactId>
    <version>1.0.0</version>
  </parent>

  <artifactId>fitconnect-sender-sample</artifactId>
  <packaging>jar</packaging>

  <name>FIT-Connect Samples - Sender</name>
  <description>
    Standalone command-line "Onlinedienst" sample that builds a submission
    from CLI-supplied data/attachments and sends it to a FIT-Connect
    destination using the FIT-Connect Java SDK.
  </description>

  <properties>
    <exec.mainClass>com.gfi.ozg.fitko.sender.SenderApp</exec.mainClass>
  </properties>

  <dependencies>
    <dependency>
      <groupId>com.gfi.ozg.fitko</groupId>
      <artifactId>fitconnect-samples-common</artifactId>
    </dependency>
    <dependency>
      <groupId>dev.fitko.fitconnect.sdk</groupId>
      <artifactId>client</artifactId>
    </dependency>
    <!-- Console binding for the SDK's SLF4J log output (retries, warnings, ...). -->
    <dependency>
      <groupId>org.slf4j</groupId>
      <artifactId>slf4j-simple</artifactId>
      <scope>runtime</scope>
    </dependency>
  </dependencies>

  <build>
    <finalName>fitconnect-sender-sample</finalName>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-shade-plugin</artifactId>
        <executions>
          <execution>
            <phase>package</phase>
            <goals>
              <goal>shade</goal>
            </goals>
            <configuration>
              <transformers>
                <transformer implementation="org.apache.maven.plugins.shade.resource.ManifestResourceTransformer">
                  <mainClass>${exec.mainClass}</mainClass>
                </transformer>
                <!-- merges SLF4J/OkHttp/etc. service files instead of letting the shade plugin keep only one -->
                <transformer implementation="org.apache.maven.plugins.shade.resource.ServicesResourceTransformer"/>
              </transformers>
              <filters>
                <filter>
                  <artifact>*:*</artifact>
                  <excludes>
                    <exclude>META-INF/*.SF</exclude>
                    <exclude>META-INF/*.DSA</exclude>
                    <exclude>META-INF/*.RSA</exclude>
                  </excludes>
                </filter>
              </filters>
            </configuration>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>
</project>
