<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>com.gfi.ozg.fitko</groupId>
  <artifactId>fitconnect-samples</artifactId>
  <version>1.0.0</version>
  <packaging>pom</packaging>

  <name>FIT-Connect Java SDK Samples</name>
  <description>
    Standalone sample clients demonstrating the FIT-Connect Java SDK: a sending
    client (Onlinedienst) and a receiving client (Verwaltungssystem). No
    application frameworks are used, only the FIT-Connect SDK and its
    transitive dependencies plus a logging facade.
  </description>

  <modules>
    <module>common</module>
    <module>sender</module>
    <module>receiver</module>
  </modules>

  <properties>
    <maven.compiler.release>11</maven.compiler.release>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

    <!-- see https://central.sonatype.com/artifact/dev.fitko.fitconnect.sdk/client
         and https://docs.fitko.de/fit-connect/docs/sdks/java-sdk/changelog for newer releases -->
    <fitconnect-sdk.version>3.5.0</fitconnect-sdk.version>
    <slf4j.version>2.0.17</slf4j.version>
    <!-- Already a transitive runtime dependency of the SDK; declared directly
         here (matching that transitive version) because the receiver sample
         calls the Jackson API itself to dump a full submission snapshot. -->
    <jackson.version>2.22.1</jackson.version>

    <maven-compiler-plugin.version>3.13.0</maven-compiler-plugin.version>
    <maven-shade-plugin.version>3.6.2</maven-shade-plugin.version>
  </properties>

  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>dev.fitko.fitconnect.sdk</groupId>
        <artifactId>client</artifactId>
        <version>${fitconnect-sdk.version}</version>
      </dependency>
      <dependency>
        <groupId>com.gfi.ozg.fitko</groupId>
        <artifactId>fitconnect-samples-common</artifactId>
        <version>${project.version}</version>
      </dependency>
      <!-- Logging facade used by the SDK; slf4j-simple provides a lightweight
           console binding so SDK log output (retries, warnings, ...) is visible.
           This is plumbing, not an application framework. -->
      <dependency>
        <groupId>org.slf4j</groupId>
        <artifactId>slf4j-api</artifactId>
        <version>${slf4j.version}</version>
      </dependency>
      <dependency>
        <groupId>org.slf4j</groupId>
        <artifactId>slf4j-simple</artifactId>
        <version>${slf4j.version}</version>
      </dependency>
      <dependency>
        <groupId>com.fasterxml.jackson.core</groupId>
        <artifactId>jackson-databind</artifactId>
        <version>${jackson.version}</version>
      </dependency>
      <dependency>
        <groupId>com.fasterxml.jackson.datatype</groupId>
        <artifactId>jackson-datatype-jsr310</artifactId>
        <version>${jackson.version}</version>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <build>
    <pluginManagement>
      <plugins>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-compiler-plugin</artifactId>
          <version>${maven-compiler-plugin.version}</version>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-shade-plugin</artifactId>
          <version>${maven-shade-plugin.version}</version>
        </plugin>
      </plugins>
    </pluginManagement>
  </build>
</project>
